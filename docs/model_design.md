# 模型设计

## 第一阶段：规则评分模型
输入年龄、ER、PR、HER2、Ki-67、ROI面积、灰度均值。输出 0-1 pCR 辅助概率、解释因子和免责声明。

## 第二阶段：传统机器学习
使用模拟或公开合规数据训练 Logistic Regression、Random Forest、XGBoost。训练时按患者划分训练集、验证集、测试集，避免同一患者影像切片泄漏。

## 第三阶段：医学深度学习预留
影像分支可使用 2.5D/3D CNN，临床分支使用MLP，最终做多模态融合。该阶段不作为MVP强制交付。
